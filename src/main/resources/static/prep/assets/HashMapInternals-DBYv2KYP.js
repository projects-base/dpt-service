import{r as z,j as s}from"./index-DW7-fKOG.js";import{u as $,S as j}from"./Stage-BiD74tSW.js";const g={w:680,h:322},k=106,d=27,w=118,A=52,E=186,x=74,v=8,p=[{key:'"alice"',hash:93110141},{key:'"bob"',hash:97714},{key:'"carol"',hash:94427452},{key:'"dave"',hash:3076010},{key:'"erin"',hash:3123477},{key:'"frank"',hash:97711389},{key:'"grace"',hash:98615748}],b=(l,a)=>l&a-1;function O(){const l=[];let a=[],i=8;const y=(e,o="new")=>{const h=b(e.hash,i),f=a.filter(m=>m.bucket===h).length;return a=[...a.map(m=>({...m,mark:void 0})),{...e,bucket:h,pos:f,mark:o}],h},r=(e,o,h,f)=>l.push({phase:e,narration:o,cap:i,nodes:a.map(m=>({...m})),hot:h,calc:f,size:a.length});r("An empty table","A HashMap is an array of buckets plus a collision strategy. Default capacity is 16 and the load factor is 0.75, so it resizes at 12 entries. The table here is drawn with 8 buckets so the chains stay readable — everything else is exactly as the JDK does it.");let t=y(p[0]);r('put("alice", …) — hash',"First the key's hashCode(), then HashMap spreads it: h ^ (h >>> 16). The bucket index is hash & (capacity − 1), which only looks at the LOW bits — so two keys differing only in their high bits would always collide. XOR-ing the high half down is what stops that.",t,[`hashCode()      = ${p[0].hash}`,"spread          = h ^ (h >>> 16)",`index = hash & (${i} − 1) = ${t}`]),t=y(p[1]),r("A second key, a different bucket","A different index, so it simply lands in its own bucket. With a good hash and a table that is not too full, this is the common case and get/put are O(1).",t,[`index = hash & (${i} − 1) = ${t}`]);const u={key:'"carol"',hash:p[0].hash&~(i-1)|b(p[0].hash,i)};t=y(u),r("Collision","Two different keys, two different hash codes, but the same INDEX — that is what a collision is. It does not mean the hashes are equal. The new entry is appended to the tail of the bucket's linked list; Java 7 prepended to the head, and the change to tail insertion is what removed the infinite-loop-on-resize bug.",t,[`carol index = ${t}`,`alice index = ${t}`,"→ same bucket, chained"]),a=a.map(e=>({...e,mark:e.bucket===t?"probe":void 0})),r('get("carol") — walking the chain',"Find the bucket, then walk it. For each node the comparison is: hashes equal first (cheap), then reference ==, only then equals(). That ordering is why a slow equals() rarely hurts — most candidates are rejected on the integer compare.",t,["1. index → bucket","2. hash == hash ?","3. key == key ?","4. key.equals(key) ?"]);for(let e=3;e<6;e++){const o={key:p[e].key,hash:p[e].hash&~(i-1)|t};y(o)}r("A chain forming",'A poor hash — or a deliberate attack — piles everything into one bucket. Now lookup in that bucket is a linear scan: O(n). This is the degenerate case that makes people say "HashMap is O(n) worst case".',t,[`bucket ${t} length = ${a.filter(e=>e.bucket===t).length}`]),r("Too long a chain — resize, or treeify?","This is the question people get wrong. A long bin does NOT automatically become a tree. treeifyBin() checks the table size first: if capacity < MIN_TREEIFY_CAPACITY (64) it calls resize() instead, on the reasoning that a small crowded table needs more buckets, not a tree. Only at 64 buckets or more does it actually build one.",t,["bin length ≥ 8 on insert","capacity < 64  → resize()","capacity ≥ 64  → treeify this bin"]),a=a.map(e=>e.bucket===t?{...e,mark:"tree"}:e),r("Treeify — one bin, not the table","With a big enough table that single bin becomes a red-black tree: O(n) → O(log n) for lookups in it. Every other bin stays a linked list. On the exact threshold: TREEIFY_THRESHOLD is 8 and the test is binCount >= 8 - 1 after appending, so you are inserting into a bin that ALREADY holds 8 — the ninth is what converts it. It orders by hash, then compareTo if the keys are Comparable, otherwise a tie-break on identity hash.",t),a=a.map(e=>({...e,mark:void 0})),r("And it can untreeify","Trees are not permanent. When a resize splits a bin into its two halves, any half left with UNTREEIFY_THRESHOLD (6) nodes or fewer converts straight back to a linked list. A tree is a local, temporary response to a bad hash distribution — not a mode the map enters.",t),a=a.map(e=>({...e,mark:void 0})),r("Crossing the load factor",`A second, completely separate reason to resize. putVal ends with if (++size > threshold) resize() — that is the load factor, and it fires however short every chain is. So resize has two triggers (load factor, and a long bin in a small table) while treeify has one. size ${a.length} against threshold capacity × 0.75 = ${i*.75}. Load factor is the trade: lower means fewer collisions and more memory, higher means a fuller, slower table. 0.75 is the JDK's compromise and is almost never worth changing.`,void 0,[`size      = ${a.length}`,`threshold = ${i} × 0.75 = ${i*.75}`,"size > threshold → resize"]);const c=i;i=16,a=a.map(e=>{const o=b(e.hash,i);return{...e,bucket:o,mark:o!==e.bucket?"moved":void 0}});const n={};return a=a.map(e=>(n[e.bucket]=(n[e.bucket]??0)+1,{...e,pos:n[e.bucket]-1})),r("Resize — and the trick that makes it cheap",`The table doubled from ${c} to ${i}. Because capacity is always a power of two, one extra bit enters the mask — so an entry either stays at index i or moves to i + ${c}, and nothing else. The JDK splits each chain into exactly those two lists without recomputing a single hash. That is why capacity is a power of two, not a prime.`,void 0,[`old index = hash & ${c-1}`,`new index = hash & ${i-1}`,`→ i  or  i + ${c}`]),a=a.map(e=>({...e,mark:void 0})),r("What breaks the contract","Mutate a field that hashCode() uses after inserting the key, and the entry is now sitting in the bucket for its OLD hash. get() computes the new hash, looks in the new bucket, and finds nothing — the entry is unreachable but still counted in size(). That is the real reason keys should be immutable, and why records and Strings make such good keys."),r("And the concurrency answer","None of this is synchronised. Two threads resizing at once can corrupt the table — in Java 7 it could spin forever in a cycle; in Java 8 you more often lose entries. The fix is not Collections.synchronizedMap, which locks the whole map: it is ConcurrentHashMap, which locks per bucket and treeifies the same way."),l}function M(){var c;const l=z.useMemo(O,[]),{frame:a,controls:i,index:y,total:r}=$(l.length,3200),t=l[a],u=t.cap;return s.jsx(j,{phase:t.phase,narration:t.narration,controls:i,index:y,total:r,stats:[["capacity",t.cap],["size",t.size],["threshold",t.cap*.75]],legend:s.jsxs(s.Fragment,{children:[s.jsx(T,{fill:"var(--accent)",label:"entry"}),s.jsx(T,{fill:"var(--boss)",label:"being touched"}),s.jsx("span",{className:"u-sans",style:{fontSize:11.5,color:"var(--ink-3)"},children:"chains grow to the right"})]}),children:s.jsxs("svg",{viewBox:`0 0 ${g.w} ${Math.max(g.h,k+u*d+16)}`,width:"100%",role:"img","aria-label":"HashMap internals",children:[(c=t.calc)==null?void 0:c.map((n,e)=>{var o;return s.jsx("text",{x:16,y:20+e*15,fill:e===(((o=t.calc)==null?void 0:o.length)??0)-1?"var(--accent-ink)":"var(--ink-3)",style:{font:"11px var(--font-mono)"},children:n},e)}),s.jsx("text",{x:16,y:k-16,fill:"var(--ink-3)",style:{font:"600 9.5px var(--font-sans)",letterSpacing:".1em"},children:"TABLE — Node<K,V>[]"}),Array.from({length:u}).map((n,e)=>{const o=k+e*d,h=t.hot===e;return s.jsxs("g",{children:[s.jsx("text",{x:w-10,y:o+15,textAnchor:"end",fill:"var(--ink-3)",style:{font:"10px var(--font-mono)"},children:e}),s.jsx("rect",{x:w,y:o,width:A,height:d-5,rx:2,fill:h?"var(--accent-soft)":"var(--surface-2)",stroke:h?"var(--accent)":"var(--line-2)",style:{transition:"fill .35s, stroke .35s"}})]},e)}),t.nodes.map(n=>{const e=E+n.pos*(x+v),o=k+n.bucket*d,h=n.mark==="probe"||n.mark==="new"||n.mark==="moved"?"var(--boss)":n.mark==="tree"?"var(--accent-ink)":"var(--accent)";return s.jsxs("g",{transform:`translate(${e}, ${o})`,style:{transition:"transform .55s cubic-bezier(.4,0,.2,1)"},children:[s.jsx("line",{x1:-v,y1:(d-5)/2,x2:0,y2:(d-5)/2,stroke:"var(--line-2)"}),s.jsx("rect",{width:x,height:d-5,rx:2,fill:h,opacity:n.mark==="tree"?.85:1,style:{transition:"fill .35s"}}),s.jsx("text",{x:6,y:15,fill:"var(--bg)",style:{font:"600 10px var(--font-mono)"},children:n.key.replace(/"/g,"")})]},n.key+n.hash)})]})})}function T({fill:l,label:a}){return s.jsxs("span",{className:"inline-flex items-center gap-1.5",children:[s.jsx("svg",{width:"11",height:"11","aria-hidden":"true",children:s.jsx("rect",{width:"11",height:"11",rx:"2",fill:l})}),s.jsx("span",{className:"u-sans",style:{fontSize:11.5,color:"var(--ink-3)"},children:a})]})}export{M as HashMapInternals};
